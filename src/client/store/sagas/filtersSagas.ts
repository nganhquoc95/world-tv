import { all, call } from 'redux-saga/effects';

// Watcher sagas for filters
// Add filter-related async operations here when needed

export default function* filtersSagas() {
  yield all([
    // Add filter watchers here
  ]);
}